# Frontend environment configuration

# Base URL for API requests. Defaults to "/api/v1" (proxied to the backend).
# In Docker Compose this is set to the backend service URL.
VITE_API_BASE_URL=/api/v1

# Dev-only: where Vite proxies "/api" requests during local development.
VITE_PROXY_TARGET=http://localhost:8000
