You are continuing development of the existing project.

IMPORTANT:

Follow existing architecture, coding standards, folder structure, reusable components, API conventions, authentication flow, ownership validation rules, notification services, and audit logging patterns.

Implement ONLY the Client Dashboard module.

Do NOT implement:

* Plan upgrades
* Subscription renewals
* Payment gateway integration
* Support tickets
* Client invoice payments

These will be implemented in later prompts.

Prerequisites already exist:

* Client Portal Foundation
* Customers
* Subscriptions
* Invoices
* Payments
* Notifications
* OTP Login
* Audit Logs

==================================================
OBJECTIVE
=========

Create a customer self-service dashboard.

The dashboard should answer:

* What connections do I have?
* When do they expire?
* Do I have unpaid invoices?
* What actions should I take?

Display only information related to the logged-in customer.

==================================================
OWNERSHIP RULES
===============

All client APIs must enforce:

customer_id = logged_in_user.customer_id

Never trust frontend filtering.

Never expose data belonging to other customers.

Do not reuse admin APIs.

Create dedicated client APIs.

==================================================
FRONTEND ROUTE
==============

Create:

/client/dashboard

Set as default landing page after CLIENT login.

==================================================
API ENDPOINTS
=============

Create:

GET /api/v1/client/dashboard/summary

GET /api/v1/client/dashboard/connections

GET /api/v1/client/dashboard/invoices

GET /api/v1/client/dashboard/notifications

GET /api/v1/client/dashboard/payments

==================================================
PERFORMANCE REQUIREMENTS
========================

Dashboard load time:

Less than 2 seconds.

Requirements:

* Backend aggregations only
* Optimized SQL queries
* Proper indexing

Do not load complete datasets.

==================================================
SUMMARY CARDS
=============

Create KPI cards.

Display:

Active Connections

Definition:

subscription.status = ACTIVE

Expiring Soon

Definition:

expiry_date <= today + 30 days

Outstanding Amount

Definition:

SUM(balance_amount)

Where:

invoice.status IN (
UNPAID,
PARTIALLY_PAID,
OVERDUE
)

Last Payment

Display:

* Amount
* Payment Date

==================================================
MY CONNECTIONS WIDGET
=====================

Display active subscriptions.

Default sorting:

expiry_date ASC

Columns:

* Connection Name
* Plan Name
* Speed
* Billing Cycle
* Expiry Date
* Days Remaining
* Status

Actions:

* View Details

Future actions:

* Renew
* Upgrade

Disable future actions for now.

Highlight:

* Expired
* Expiring within 7 days

==================================================
RECENT INVOICES WIDGET
======================

Display latest invoices.

Default sorting:

created_at DESC

Limit:

10 records

Columns:

* Invoice Number
* Connection Name
* Invoice Date
* Due Date
* Total Amount
* Status

Actions:

* View Invoice
* Download PDF

Highlight:

* Overdue invoices

==================================================
OUTSTANDING INVOICES WIDGET
===========================

Display only invoices where:

balance_amount > 0

Default sorting:

due_date ASC

Columns:

* Invoice Number
* Due Date
* Outstanding Amount
* Days Overdue

Actions:

* View Invoice

Highlight:

* Due within 7 days
* Overdue

==================================================
RECENT PAYMENTS WIDGET
======================

Display latest payments.

Default sorting:

payment_date DESC

Limit:

10 records

Columns:

* Payment Date
* Invoice Number
* Connection Name
* Amount
* Payment Method

==================================================
NOTIFICATIONS WIDGET
====================

Display recent notifications sent to the customer.

Include:

* Welcome messages
* Invoice notifications
* Payment confirmations
* Expiry reminders

Limit:

10 records

Columns:

* Date
* Title
* Channel
* Status

Default sorting:

created_at DESC

==================================================
QUICK ACTIONS
=============

Display:

* View Connections

* View Billing

* Download Latest Invoice

* Update Profile

Future actions:

* Renew Subscription

* Upgrade Plan

Disable future actions.

==================================================
EMPTY STATES
============

Connections:

"No active connections found."

Invoices:

"No invoices available."

Payments:

"No payment history available."

Notifications:

"No notifications available."

==================================================
UI REQUIREMENTS
===============

Reuse existing theme.

Primary:

#1F4959

Secondary:

#5C7C89

Use:

* shadcn/ui cards
* Existing DataTable component

Requirements:

* Fully responsive
* Mobile friendly
* Loading skeletons
* Error states
* Empty states

Desktop:

4-column KPI grid.

Tablet:

2-column grid.

Mobile:

Single-column layout.

==================================================
SECURITY REQUIREMENTS
=====================

Prevent:

* ID enumeration
* Access to other customer data

Use UUIDs only.

Enforce ownership checks in backend services.

Log unauthorized access attempts.

==================================================
AUDIT LOGGING
=============

Track:

* client_dashboard_viewed

* client_invoice_downloaded

* unauthorized_client_access_attempt

==================================================
TESTING
=======

Create tests for:

* Ownership validation

* Dashboard aggregations

* Active connection counts

* Outstanding amount calculations

* Expiring subscription logic

* Recent invoice queries

* Recent payment queries

* Notification queries

* Unauthorized access attempts

==================================================
ACCEPTANCE CRITERIA
===================

Success means:

1. Dashboard loads within 2 seconds

2. Customers see only their own data

3. KPI values are accurate

4. Connections display correctly

5. Outstanding amounts are accurate

6. Invoice downloads work

7. Dashboard is responsive

8. Empty states work

9. Unauthorized access is blocked

10. Audit logs are created
